import React from 'react';
import PropTypes from 'prop-types';

const ForecastDetails = (props) => {
  return <div className="forecast-details" data-testid="forecast-details" />;
};

export default ForecastDetails;
